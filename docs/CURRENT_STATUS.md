# CURRENT_STATUS.md — PetLand v22

## وضعیت کلی
**عملیاتی** — ربات در Liara deploy شده و در حال سرویس‌دهی است.

---

## ویژگی‌های پیاده‌سازی‌شده ✅

| ویژگی | وضعیت |
|-------|-------|
| دریافت پیام از Bale (long polling) | ✅ کامل |
| منوی اصلی و ناوبری | ✅ کامل |
| نمایش دسته‌بندی محصولات | ✅ کامل |
| نمایش جزئیات محصول + عکس | ✅ کامل |
| سبد خرید (افزودن، مشاهده) | ✅ کامل |
| فرم ثبت سفارش (7 مرحله) | ✅ کامل |
| آدرس‌های ذخیره‌شده در checkout (حداکثر ۳) | ✅ کامل |
| پرداخت دستی کارت به کارت + شبا (پایا) | ✅ کامل |
| آپلود رسید پرداخت | ✅ کامل |
| مشاهده جزئیات سفارش با کد پیگیری | ✅ کامل |
| ادامه پرداخت برای سفارش‌های WAITING_PAYMENT | ✅ کامل |
| اطلاع‌رسانی به ادمین هنگام سفارش | ✅ کامل |
| دریافت و پردازش callback_query (inline buttons) | ✅ کامل |
| پنل ادمین — مدیریت سفارش | ✅ کامل |
| تأیید/رد سفارش توسط ادمین | ✅ کامل |
| ارسال سفارش (اسنپ یا پست) | ✅ کامل |
| ارسال PDF فاکتور به ادمین هنگام تایید | ✅ کامل |
| آمار فروش ماهانه در پنل ادمین (۶ ماه) | ✅ کامل |
| مدیریت تیکت پشتیبانی | ✅ کامل |
| حالت همکار (عمده‌فروشی) | ✅ کامل |
| seed محصولات (~130+ آیتم) | ✅ کامل |
| health check endpoint | ✅ کامل |
| سیستم بازاریابی و لینک معرفی (پشت کد مخفی) | ✅ کامل |
| ثبت معرف برای کاربران جدید (deep link) | ✅ کامل |
| کیف پول کاربر (پشت کد مخفی) | ✅ کامل |
| واریز 5٪ پورسانت به معرف هنگام تایید سفارش | ✅ کامل |
| درخواست برداشت از کیف پول | ✅ کامل |
| مدیریت درخواست‌های برداشت در پنل ادمین | ✅ کامل |
| اطلاع‌رسانی کد رهگیری برداشت به کاربر | ✅ کامل |
| حذف خودکار پیام‌های قبلی (clean chat) | ✅ کامل |
| پشتیبانی از `/start ref_xxx` (referral deep link) | ✅ کامل |

---

## موارد ناقص / نیمه‌کاره ⚠️

| مورد | توضیح |
|------|-------|
| فاکتور PDF فارسی | PDFKit متن فارسی را درست render نمی‌کند — نیاز به pdfmake یا puppeteer |
| PDF به کاربر | فاکتور PDF فقط به ادمین می‌رود |
| Webhook | فایل `src/bot/webhook.js` وجود دارد ولی stub است |
| `axios` | در `package.json` وجود دارد ولی استفاده نمی‌شود |
| تغییر `profitPercent` محصول | فقط از طریق DB مستقیم — دستور ادمین در ربات وجود ندارد |

---

## موارد وجودنداشته / Missing ❌

| مورد | اهمیت |
|------|-------|
| `.gitignore` | بالا — خطر push کردن `.env` |
| `README.md` | متوسط |
| Migration files (Prisma) | بالا — rollback امکان‌پذیر نیست |
| تست‌های خودکار | بالا |
| Rate limiting | متوسط |
| Logging سیستماتیک | متوسط |

---

## آخرین وضعیت فایل‌ها

| آمار | مقدار |
|------|-------|
| فایل‌های سورس | **30 فایل** |
| مدل‌های DB | **12 مدل** |
| محصولات catalog | **~130+ آیتم در 14+ دسته** |
| متغیرهای محیطی | **14 متغیر** |

## وضعیت کلی
**عملیاتی** — ربات در Liara deploy شده و در حال سرویس‌دهی است.

---

## ویژگی‌های پیاده‌سازی‌شده ✅

| ویژگی | وضعیت |
|-------|-------|
| دریافت پیام از Bale (long polling) | ✅ کامل |
| منوی اصلی و ناوبری | ✅ کامل |
| نمایش دسته‌بندی محصولات | ✅ کامل |
| نمایش جزئیات محصول + عکس | ✅ کامل |
| سبد خرید (افزودن، مشاهده) | ✅ کامل |
| فرم ثبت سفارش (7 مرحله) | ✅ کامل |
| پرداخت دستی کارت به کارت + شبا | ✅ کامل |
| آپلود رسید پرداخت | ✅ کامل |
| مشاهده جزئیات سفارش با کد پیگیری | ✅ کامل |
| ادامه پرداخت برای سفارش‌های WAITING_PAYMENT | ✅ کامل |
| اطلاع‌رسانی به ادمین هنگام سفارش | ✅ کامل |
| دریافت و پردازش callback_query (inline buttons) | ✅ کامل |
| پنل ادمین — مدیریت سفارش | ✅ کامل |
| تأیید/رد سفارش توسط ادمین | ✅ کامل |
| ارسال سفارش (اسنپ یا پست) | ✅ کامل |
| ارسال PDF فاکتور به ادمین هنگام تایید | ✅ کامل |
| مدیریت تیکت پشتیبانی | ✅ کامل |
| حالت همکار (عمده‌فروشی) | ✅ کامل |
| seed محصولات (~130+ آیتم) | ✅ کامل |
| health check endpoint | ✅ کامل |
| سیستم بازاریابی و لینک معرفی | ✅ کامل |
| ثبت معرف برای کاربران جدید (deep link) | ✅ کامل |
| کیف پول کاربر (موجودی + تاریخچه) | ✅ کامل |
| واریز 5٪ پورسانت به معرف هنگام تایید سفارش | ✅ کامل |
| درخواست برداشت از کیف پول | ✅ کامل |
| مدیریت درخواست‌های برداشت در پنل ادمین | ✅ کامل |
| اطلاع‌رسانی کد رهگیری برداشت به کاربر | ✅ کامل |
| حذف خودکار پیام‌های قبلی (clean chat) | ✅ کامل |
| پشتیبانی از `/start ref_xxx` (referral deep link) | ✅ کامل |

---

## موارد ناقص / نیمه‌کاره ⚠️

| مورد | توضیح |
|------|-------|
| فاکتور PDF فارسی | PDFKit متن فارسی را درست render نمی‌کند — نیاز به pdfmake یا puppeteer |
| PDF به کاربر | فاکتور PDF فقط به ادمین می‌رود — باید بعد از تایید به کاربر هم ارسال شود |
| Webhook | فایل `src/bot/webhook.js` وجود دارد ولی stub است |
| `axios` | در `package.json` وجود دارد ولی استفاده نمی‌شود |
| مدیریت خطا | جزئی است — crash ممکن است polling loop را متوقف کند |

---

## موارد وجودنداشته / Missing ❌

| مورد | اهمیت |
|------|-------|
| `.gitignore` | بالا — خطر push کردن `.env` |
| `README.md` | متوسط |
| Migration files (Prisma) | بالا — rollback امکان‌پذیر نیست |
| تست‌های خودکار | بالا |
| Rate limiting | متوسط |
| Logging سیستماتیک | متوسط |

---

## آخرین وضعیت فایل‌ها

| آمار | مقدار |
|------|-------|
| فایل‌های سورس | **30 فایل** |
| مدل‌های DB | **10 مدل** |
| محصولات catalog | **~130+ آیتم در 14+ دسته** |
| متغیرهای محیطی | **13 متغیر** |
